# Pinned to the versions this PoC was developed and measured against.
# Python 3.13. Install with:  pip install -r requirements.txt

# LangChain v1 — note EnsembleRetriever lives in langchain-classic since v1.
langchain==1.3.16
langchain-core==1.6.0
langchain-community==0.4.2      # FAISS vectorstore, BM25Retriever
langchain-openai==1.6.0         # OpenAI-compatible client (points at Ollama, not OpenAI)
langchain-classic==1.0.8        # EnsembleRetriever

faiss-cpu==1.15.0               # exact brute-force search; no server to deploy
rank-bm25==0.2.2                # BM25Retriever backend
python-dotenv==1.2.3

# Pulled in transitively, pinned for reproducibility.
openai==3.3.1
numpy==2.5.2
